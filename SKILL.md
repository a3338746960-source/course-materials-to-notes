---
name: obsidian-course-vault
description: 把大学课程的原始材料（课件 PPT/PDF、syllabus、电子课本、课堂录音转写）整合成 Obsidian 课程知识库，用于考试复习与课后快速回顾。覆盖 Vault 布局、证据等级判定（书面优先于录音）、中文化的标签体系与英文原句降级规范、「全中文模式」（连文件名一起改中文）、讲义/概念/周复习三类笔记模板、材料压缩规则、老式 .ppt / .doc 文本提取工具链、批量中英改写引擎（relang 系列 / zhify 系列）、同名文件链接消歧、教务课表融入与「笔记完全性审查」。当用户要求整理课程材料、建 Obsidian 课程笔记、把课件转成复习笔记、嫌笔记「英文太多」要求中文化、要求某门课「用全中文」、要求把课表融进笔记或审查笔记是否完整、或提到「Course Hub」「Lecture Note」「Weekly Review」时使用。
---

# 大学课程知识库整理流程

目标：**Low Maintenance + High Review Value + Course-Material Grounded**。
优先级：**准确性 > 考试/学习价值 > 清晰度 > 完整性 > 笔记数量**。
压缩目标：保留原课程信息中约 **40–60%** 的学习价值，不追求全量转写。

> 一句话概括：**把一学期几十份课件、一本课本、几十小时的录音，压成一棵两天就能过一遍的复习树。**

---

## 0. 先确认三件事，再动工

1. **Vault 根目录**（决定 `![[xxx.pdf#page=N]]` 能否解析）——必须是所有原始材料的**共同上级目录**，且原始材料**原位保留不移动**。
2. **起步课程**——先跑通一门验证格式，再批量推进。一次性建 7 门课的框架会得到一堆空壳。
3. **语言策略**——默认按第 2 节**默认档**执行（中文标签 + 术语英文 + PPT 原句降级），并向用户确认是否接受"PPT 英文原句保留为引用附注"。
   ⚠️ 若用户说"**用全中文**"，走第 2 节 ④ **全中文档**：**文件名也改中文**，术语一律中文、仅留公式与专名。但**必须先问清适用范围**——"全中文"通常**不等于全库推行**（实测：用户只指定了 2 门课，其余 5 门明确"不变"），并确认"专名是否保留原文"。

---

## 1. Vault 布局

推荐把顶层结构定为「**课程 → 材料分类**」（反向的「材料分类 → 课程」也能用，但结构变动后必须复跑路径审计，见下）：

```
<根目录>/                          ← Vault 根 = 所有原始材料的共同上级，含 .obsidian
├── <课程名>/                       ← 顶层是课程
│   ├── 课件/                       ← 材料分类在课程之下
│   ├── 课堂录音纪要/
│   └── 课本/
├── <课程名>/
│   └── ...
└── University/                     ← 笔记单独一支，与原始材料并列
    ├── <课程名>/
    │   ├── Course Hub.md           ← 导航页（不搬知识内容）
    │   ├── Lectures/LXX - Topic.md
    │   ├── Concepts/Concept Name.md
    │   └── Weekly Reviews/Week XX Review.md
    ├── 考试与截止日期总览.md        ← 跨课程：考试与截止（可选，需用户确认）
    └── 学期课表.md                  ← 跨课程：每周课表 + 笔记完全性审查（可选，需用户确认）
```

⚠️ **顶层结构一旦改动，必须全库复跑"路径引用审计"**：「材料分类 → 课程」与「课程 → 材料分类」两种都能用，选哪个取决于用户，但**中途换向会让笔记里所有写死的路径串失效**。

- **不创建空的 `Lectures/`、`Concepts/`、`Weekly Reviews/` 目录**，待有内容时惰性创建；**不建空的 `Materials/` 目录**（原材料已在 Vault 内按课程组织，重复建目录只会产生空文件夹与双份定位）。
- ⚠️ **材料分类目录允许"因课而异"，不要强行补齐**：并非每门课都有 `课件/`、`课本/`、`课堂录音纪要/` 全套。实测某门课**只有 `课堂录音纪要/` + `作业讲解/`**（`作业讲解/` 是其余各课都没有的分类），且**无 `课件/`、无 `课本/`**。此时**不新建空目录凑全**，只在 Course Hub 的「原始材料位置」段注明该课的目录差异与材料缺口。
- ⚠️ **目录名也要与全库惯例对齐**：若某课录音目录写成 `录音纪要/`，而全库统一叫 `课堂录音纪要/`，**先问用户是否改名**，再动（改名是"高阶危险操作"，见第 4 节）。
- **材料文件本身不移动、不重命名**（Obsidian 按 basename 全库解析 `![[file.pdf#page=N]]`，挪目录不影响嵌入）。
- ⚠️ **但笔记里写死的路径字符串会因结构调整而失效** ⇒ **每次用户调整结构后，必须全库复跑一次路径审计**，把 `` `课件/<课程>/` `` 这类引用改成实际结构。审计方法：检索 `` `(课件|课堂录音纪要|课本)/ `` 后逐一核对。实测一次结构调整影响过 7 个主页、15 行。
- ⚠️ **vault 根不要嵌套在另一个 git 仓库里**，也不要把原始材料（他人版权物）推送到公开仓库。

## 1.1 ★ 证据纪律的九条补充判据

前四条是基础，5–9 条是踩坑换来的：

1. **整理版纪要可能是 AI 生成的**：若纪要中出现 `【AI 注释】`、`【笔记】`、"待查证盲区"之类小节——**这些是生成方推断，不是课程材料，一律不得采信**（实例：某纪要写"图中未标注指向操作 X 的路径，故补充此隐含路径"，纯属推断）。照抄"待查证盲区"进笔记时必须标注**未经核实**。
2. **老师可能未记录第 1 次课**：拿到"纪要1"不等于"第 1 讲"。先核对讲次口径——若录音中出现"现在第 N 周""上节课我说过"，说明该课已上过更多次，**文件序号 ≠ 课次**。笔记命名与覆盖边界必须按**实际课次**交代清楚（如"第一讲横跨第 1、2 周；第 1 周课无录音"）。
3. **同一门课的材料可以互相裁定**：当"课程目标"与"考核方式"写法冲突时，去**课件（PPT/PDF）**里找第三方书面证据——课件往往直接写明考核构成，常能打破 syllabus 内部的两处矛盾（实例：某课期末"研究设计 vs 开卷考试"之争，由课件考核页定为开卷考试）。
4. **材料路径引用要用反引号写全**，便于结构调整后批量修正。
5. ★ **老师会主动标记考点——务必逐字抓取**。处理录音原文时**必检关键词**：`必考` / `考点` / `考试点` / `不考` / `不是考试的点` / `一定要考` / `会考` / `考试肯定要出`。这类措辞的**证据等级相当于书面材料**，是无 syllabus 课程里最可靠的情报。
   - 实证：某课老师原话"**这就是我们的必考题……期末考试肯定要出的题**"，同时**主动排除**了两处（"这个意象保留不是考试点""平行文本也不是考试的点"）。
   - ⇒ **负面信息与正面信息同等重要**（"这不是考点"直接省下复习时间），必须一并写入"评估信号"段。
   - ⚠️ **但"考试"二字本身不等于情报**：老师说"我们的考试基本上都是计算题"时，必须结合**上下文**判断其范围（该例中指的是理论公式计算，不是算税额）。**不可脱离语境直接引用**。
6. ⚠️ **"两份书面材料互相冲突"是"书面 > 录音"原则覆盖不到的情况**：实测到**教学大纲（syllabus）与教师讲义（PPT/PDF）对同一考核构成给出不同比例**（总额一致，仅内部两项对调）。此时应按**权威层级**处理——**教学大纲 > 教师讲义**，并**双列对照表 + 留一个"待老师确认"标记**，不要静默择一。
7. ⚠️ **文件时间戳只能当"线索"，不能反推上课日期**。见 §1.2——这是一条**先被提出、又被自己的数据推翻**的规律，教训比结论更重要。
8. ⚠️ **AI 生成的纪要与课件冲突时，必须回逐字稿核实"老师到底说没说过"**。
   - 实证：整理版纪要写"某市场普遍处于**半强式有效**"，而**课件写明"尚未达到半强式"**、多数研究支持已达到弱式；回听录音后发现**老师本人根本没下这个结论**——他只是让学生举手，随后强调的是"价格与价值偏离程度相对比较大"。
   - ⇒ 处理顺序：**① 找课件对位页（书面优先）→ ② 在逐字稿里检索该结论的关键词并计数 → ③ 若逐字稿里没有，就在笔记里显式写出"纪要如此写，但课件与录音均不支持，本库不采用"**。**不要静默采用纪要的结论，也不要静默删掉它——要把冲突写出来。**
9. ⚠️ **"把课件里有的写成老师讲过的"是本流程最高频的错误**——实测已抓到多例（同一门课的 L01 两次）。**可执行的复核方法**：在逐字稿（`extract.py` 抽出的 txt）里**对关键术语做 grep 计数**——`grep -c 术语 原文.txt`。**计数为 0 = 该术语本讲未出现 = 不得写成"已讲授"**。每次整理完一讲，挑 3–5 个课件里出现、但你准备写进"已讲授"的术语跑一遍。反面代价极低（一次 grep），正面价值很高（避免复习时对着没讲过的内容空耗）。

## 1.2 ⚠️ 讲课日期的判定：**一条被自己的数据推翻的规律**

**曾提出的规律**：`纪要入库日 = 上课日`。提出时手上有 **5 处一致证据、覆盖 3 门课**，看起来"可靠度较高"。

**⚠️ 次日把全库时间戳 × 课表节次拉通交叉比对，同一批数据里就找到 3 处反例，该规律作为"推定法"已被推翻：**

| 材料 | 纪要入库时间 | 该课实际上课时间 | 结论 |
|---|---|---|---|
| 课 A 的 L01 | 周四 | **周二** | ❌ 入库日 ≠ 上课日 |
| 课 B 的 L02 | 周二 | **周四** | ❌ 入库日 ≠ 上课日 |
| 课 C 的 L02 | 周一 | **前一个周五** | ❌ 入库日 ≠ 上课日 |

原"5 处成立"的解释是**巧合**：那 5 处的入库日恰好都落在上课日当天或之后最近一次课附近。**样本一扩大就崩。**

**⇒ 正确用法（三条，按优先级）**：

1. **首选：直接问用户**。上课时间、课次编号都属"用户一句话就能确认、材料推不出来"的信息。**不要用材料硬猜。**
2. **次选：与已知课表交叉比对**。若入库日与课表节次**冲突**，取**该入库日之前最近的那个上课日**。这只是**排除法**，仍须标注推定。
3. **辅助：课件时间戳落在课时之内**（例：课件入库 10:33，而该次课为 10:00–11:40）⇒ 课件是当堂发的，可**独立确认**该日的课确实存在。

⚠️ **纪律**：

- 推定出的日期在 frontmatter 写 `date: YYYY-MM-DD（推定）`，正文一句话交代**依据**，末尾加"若日期有误请告知，我改"。
- **不要用时间戳猜"隔了多久"**：它最多定"哪一天"，定不了"第几次课"。**课次编号必须向用户确认。**
- ⚠️ **绝不允许用时间戳把一门课"补进课表"**。课表这类事实只认两条来源：**教务课表（书面）**与**用户确认**。
- ⚠️ **真正的教训**：**样本量小（3 门课 5 处）时，"规律成立"极可能是巧合。** 任何准备写成"规律"的文件时间戳 / 命名模式，都要**先主动去找反例**再写进流程。**一条写错的"规律"比没有规律更有害**——它会让后来的人不再核实。

---

## 2. 语言规范（阅读路径全中文）

用户诉求通常是"复习时快速看懂"，不是"保存原文"。**中文占比高不等于好读——关键是英文别出现在阅读路径上。**

> 实测经验：一份中文字符占比已达 88% 的讲义，用户仍反馈"英文太多不好理解"。原因不是**量**，而是英文落在**结构性位置**上（小节标题、表格列头、字段标签），它们是阅读时最先撞到的东西。

⚠️ **两个档位，按课生效，不要全库一律**：

| 档位 | 适用范围 | 文件名 | 术语 |
|---|---|---|---|
| **默认档**（①②③） | 全库默认 | 保留英文（`Course Hub.md` / `Week XX Review.md` / `LXX - Topic.md`） | 本体保留英文（MSB / DWL / EMH） |
| **全中文档**（④） | **用户逐课指定** | **改中文**（`课程主页.md` / `第 XX 周复习.md` / `第 0X 讲 - ….md`） | **一律中文**（仅公式记号与专名保留） |

**默认档的三条硬规则：**

### ① 结构标签一律中文

| 位置 | 用这些中文标签（不要留英文） |
|---|---|
| 讲义小节 | 快速回顾 / 核心概念 / 核心框架与模型 / 老师补充 / 评估信号 / 关联 |
| 概念页小节 | 定义 / 核心理解 / 构成要素 / 框架 · 图示 / 出现于 / 老师补充 / 评估相关性 |
| 主页小节 | 课程简介 / 先修要求 / 课程目标 / 教材 / 考核方式 / 课程进度 / 讲次 / 核心概念 / 周复习 / 课程信息 / 原始材料位置 |
| 周复习小节 | 一、本周讲了什么 / 二、核心概念 / 三、讲次之间的关系 / 四、必掌握的框架 / 考核关注点 |
| 表格列头 | 考核项 / 权重 / 截止 / 状态；讲次 / 主题 / 状态；周次 / 主题 / 状态 |
| 状态值 | `已讲授` / `课件已到位` / `计划中` / `待定` |
| 字段标签 | **定义**：/ **用途**：/ **构成**：/ **如何使用**：/ **关键性质**：/ **为什么重要**：/ **现实案例**： |

### ② 术语本体保留英文（仅默认档；全中文档见 ④）

概念名与缩写（MSB / MSC / MPC / MPB / MEC / MEB / DWL / ELR）、框架与模型名（SWOT / PEST / BCG）、公式记号、教材与期刊名、**讲次与章节主题名**（用于对照 syllabus / 教材）。

首次出现写 `English Term（中文解释）`，其后可用英文缩写。**不为双语而双语**——不要同一句翻译两遍。

### ③ 英文原句降级为引用附注

```
- **定义**：纳税人在价格扭曲税下缴税、而非在 lump-sum tax 下缴纳同等收入时，所损失的福利。

  > PPT 原文：the loss in well-being when a taxpayer pays taxes under a price-distorting tax instead of under a lump-sum tax.
```

**为什么不能直接删掉英文原句**：若课程为英文授课且期中/期末覆盖这些讲次，PPT 原句就是**考试卷面用语**的来源。删了读着舒服，但会丢术语熟悉度。降级为引用既清空阅读路径，又保留卷面用语。

**何时可以彻底不留**：中文授课课、且该讲不在英文考试范围——但仍需用户确认。

### ④ 全中文模式：文件名也改中文（**仅用户指定课程**）

用户可能对**某几门课**要求"**用全中文**"，并追加两条口径：**文件名全部改中文（含 Hub 与周复习）**、**术语全中文、专名保留原文**。⚠️ 这句话**默认只对指定课生效**，不要全库推行。

| 原文件名 | 新文件名 |
|---|---|
| `Course Hub.md` | `课程主页.md` |
| `Week XX Review.md` | `第 XX 周复习.md` |
| `LXX - Topic.md` | `第 0X 讲 - Topic.md` |
| `Concepts/Event Study.md` | `Concepts/事件研究法.md` |

**术语一律中文，但译名必须回课件取证，不得凭印象自创。** 做法：用 `pdfplumber` 抽该课 PDF、数中文原词的出现次数，**以课件写法为准**。实测差异举例：课件写「贝努利」而不是教科书常见的「伯努利」、写「分布律」而不是「分布列」、写「累积异常收益」而不是「累计异常收益」。**同义词误译会在考试时对不上卷面用语**——这类差异凭印象是猜不出来的，只能去课件里数。

**只有两类保留原文**：① 公式与数学记号（`$AR_t$`、`t = 0`）；② **专名**——文献名（`Ball & Brown (1968)`）、数据库（`CSMAR`）、软件（`Stata`）、学者（`Fama` / `Libby`）、期刊名、课件文件名。**目录名 `Weekly Reviews/` 保留英文**（改名收益低、破坏面大）。

**⚠️ 三个必踩的坑：**

1. ⚠️ **术语规则会改写链接路径——本档最凶的一次事故**：规则表里写了 `Weekly Reviews → 周复习`，结果把 `[[某课/Weekly Reviews/第 03 周复习]]` 改成 `[[某课/周复习/…]]`，**一次改坏 9 条链接**。⇒ **根治：把 `\[\[[^\]]*\]\]` 纳入保护段**（`![[...]]`、`` `...` ``、`$$…$$`、`$…$`、URL 同办），脚本永不触碰链接内部；改完必须复跑全库链接审计。
2. ⚠️ **改名会让"新文件名"自己成为重名组**：多门课同时改出 `课程主页.md` / `第 02 周复习.md` / `第 03 周复习.md`，三组各增 2 个同名；全库重名组从 5 组升到 7 组。⇒ **改名后必须全库复跑歧义审计**，并把**所有**指向旧名的链接（含**跨课程页**）逐条改为新名。
3. ⚠️ **`\b` 在中文相邻处失效**：`\bAPI\b` 匹配不到 `API刻`（中文字符属 `\w`）。凡术语规则用了 `\b`，一律换成 `(?<![0-9A-Za-z])` / `(?![0-9A-Za-z])`。（**不要**写 `(?<=^|[|(])\b`——会报 *look-behind requires fixed-width pattern*；改用手动扫描字符串逐处替换。）

**英文括注要成对删掉**：中文化后残留 `概念（English Term）` 可达 **103 处**。作法：在术语规则**之后**再跑一条「删英文括注」规则，用前置字符断言防误伤，并以 `KEEP_PAREN` 豁免邮箱、`t = 0`、`X/Y/Z`、`BB 2019`、`Ball & Brown`、`Robert Libby` 等必须留括号的场合。另需**专名遮蔽（MASKS）**：文献标题（如 *An empirical evaluation of accounting income numbers*）先换成哨兵再还原，否则标题内部的普通词会被规则误译。

**收尾三连（缺一都会留隐患）**：① 清 frontmatter 里 `aliases` 的纯英文项（正文规则碰不到 frontmatter）；② 与改前备份**逐文件对账 行/嵌图/公式/链接 四项计数**；③ 扫重复译名（双重替换产物，如「盈余后漂移盈余后漂移」）。已固化为 `tools/zh_check.py`，见 §11.1。

### 其他语言约定

- 规格占位句：`⚠️ 课程材料未提供足够信息，无法清楚解释这一点。`
- 无纪要讲次的结论句：`未发现明确的评估信号。`
- 引用块内的英文、图注里的 Figure 标题、老师原话——**保留原文**，这些是"证据"不是"叙述"。
- frontmatter 字段名保留英文（供 Obsidian 索引），值用中文。

**自检**：改写后统计"阅读路径中文占比"（排除 `>` 引用块、`![[` 嵌图、`|` 表格行），**默认档应达 85%+**，低于此值说明还有英文落在正文里。可直接用 `tools/relang.py` 的 `--stats` 模式统计。

**全中文档（④）的实测基准：97.5% / 93.0%，两课合计 95.1%**——剩下的拉丁**全部**是 frontmatter 字段名、公式数学记号、专名与文件名，属允许残留；若检出的仍是普通术语英文，说明规则表漏了一条。

---

## 3. 证据等级（本流程最容易出错的地方）

严格区分五种状态，**不得混淆**：

| 状态 | 判据 |
|---|---|
| `计划中` | syllabus 表明未来会涉及 |
| `课件已到位` | PPT/PDF 已存在，**但无证据表明老师已讲授** |
| `已讲授` | 有课堂 transcript、用户笔记或其他明确证据可确认已上过 |
| 老师强调 | 老师在课堂中明确重复 / 强调 / 特别解释 |
| 与考核相关 | 存在明确课程证据（老师说会考、syllabus 明述、assessment brief 涉及） |

- **只有 PPT 时**：不得写"老师强调 / 老师解释 / 老师说这要考"。只能写"基于课件"或直接总结 PPT。
- **禁止**从"这个概念看起来很重要"推断"可能会考"。
- 未来 PPT **不得**当成已上过的 Lecture。
- ⚠️ **"某讲已讲授" ≠ "该章讲完了"**：一门课的**第一次课很可能是课程导论**，只覆盖第一章的界定部分，其余内容老师会明说顺延。⇒ 处理方式：
  1. 在**讲义与主页双处**显式声明覆盖边界；
  2. 未讲授的课件页**不嵌入**（嵌入会暗示"老师讲过这一页"）；
  3. **佐证方法**——翻下一份课件的开篇"复习 / Review"页，上面列出的条目往往正是老师认定"已经讲过"的内容。

### 源可靠性原则

**书面材料 > 课堂录音。** 音频转写存在口述与转写噪音（实测：把教材作者名听成读音相近的中文、deadweight loss 记成 "dead loss"、评估权重说成与 syllabus 不符的数字、把某处数字说反）。

凡录音与 syllabus / PPT / 官方文件冲突，**一律以书面为准**。**不要**因为"整理版纪要与原始逐字稿两处一致"就提高其可信度——**两者同源，会一起错。**

由录音衍生的**数值**（金额、比例、税率、日期）使用前须核对书面材料；录音的**定性解释、案例、职业信息**可作为"老师补充"保留，但需在段首加可靠性提示。

⚠️ **降级要降全**：一旦裁定某份录音不准，**所有**引它作唯一依据的信号（讲义正文、概念页、周复习、主页）都要同步降级为"仅录音来源，待书面确认"。只在讲义里降级、概念页忘了改，是本流程实际踩过的坑。

⚠️ **但录音也可能反过来印证书面**：老师口头说出的日期若与 syllabus 的 tentative 日期一致，可信度应**上调**，记得同步回写主页与总览页。

### 材料优先级

1. **PPT / PDF** — 确定 structure / definitions / models / frameworks / tables / figures / formal terminology
2. **整理版 transcript** — 补充 professor's explanation / examples / clarifications / emphasis
3. **原始 transcript** — **不默认通读**。仅在以下情况查看：整理版明显缺失 / 需核实重要概念 / 整理版与 PPT 冲突 / 需确认老师是否真的强调某事 / 关键解释语义不清

### Source Conflict

不自行择一采信，标记（标签用中文）：

```
⚠️ 材料冲突 —— <主题>
材料 A（出处）：…
材料 B（出处）：…
待确认：…
```

同一份 syllabus **内部**矛盾同样适用。若冲突能由第三方书面材料裁定（见 1.1 第 3 条），则改为写 `✅ 已裁定` + 判读依据，并保留原始分歧供溯源。

---

## 4. 链接与命名的消歧规则（多课同名文件必踩）

每门课都有 `Course Hub.md` → `[[Course Hub]]` 在 Obsidian 中**歧义**（同名文件路径距离相同，解析结果不可预测）。

- **凡指向同名文件的链接，一律写路径限定形式**：`[[<课程名>/Course Hub|课程主页]]`
- ⚠️ **这个风险会"事后引爆"**：新增一个同名文件，会让**既有笔记里原先靠唯一性侥幸成立的旧链接**立刻变成歧义（实测：某课新增 `Week 01 Review.md` 后，另一课主页 2 处 `[[Week 01 Review]]` 当即失效）。⇒ **每新建一个与既有文件同名的笔记，必须全库复跑一次歧义审计**：找出所有 `[[X]]` 而 `X` 恰为同名 basename 的情况，改为路径限定。
- ⚠️ **批量讲课会放大该风险**：一轮同时处理 3 门课，就会**同时**新增 3 个 `Week 02 Review.md`，同名数从 3 直接跳到 6。⇒ **批量建课后审计绝不能省**；也不要因为"我写的都是路径限定形式"就跳过审计——**旧笔记里的链接才是真正会炸的那批**。
- 实测重名规模（**82 篇笔记**）：`Course Hub` ×6、`Week 02 Review` ×6、`Week 01 Review` ×3、`Externality` ×2、`第 02 周复习` ×2、`第 03 周复习` ×2、`课程主页` ×2 —— 共 **7 组**。审计通过的标准是 **未解析 = 0 且歧义 = 0**（终值 **510 条链接 / 0 未解析 / 0 歧义 / 0 路径失效**，嵌图 75 处）。
- ⚠️ **重名规模会随语言档位变化**：把两门课转为**全中文档**（§2 ④）后，`Course Hub.md` 从 ×8 降到 ×6，但**新造出** `课程主页` / `第 02 周复习` / `第 03 周复习` 三组各 ×2。⇒ **"改名"与"新建同名文件"对链接的风险完全同构**，两种操作之后都必须全库复跑审计。
- ⚠️ **审计脚本的解析口径必须照抄 Obsidian，否则会大面积误报**：Obsidian 对 `[[A/B]]` 按**路径后缀**匹配（任何以 `A/B.md` 结尾的文件都算命中），**不是**从 Vault 根做前缀精确匹配。实测：写成前缀精确匹配后，一次性误报 **345 处"未解析"**，而真实未解析是 0。
  - 正确判定顺序：**① 路径后缀唯一命中 → 通过；② 否则 basename 唯一 → 通过；③ basename 重名 → 歧义；④ 都没有 → 未解析。**
- ⚠️ **审计脚本另有三处必做处理（否则会误报或掩盖真问题）**：
  1. **先剔除 `![[...]]` 再解析 `[[...]]`**——嵌图不是 wiki 链接（实测：不剔除会一次性报 48 个"未解析"）。
  2. **再剔除行内代码 `` `...` `` 里的内容**——笔记里常把 `![[文件名]]` 当**示例**写在反引号里（解释 Obsidian 解析机制时），脚本会把示例当成真引用，报出"找不到 `文件名`"这样的假告警。**Obsidian 不渲染行内代码里的嵌入，所以那是无害的；但脚本必须跳过它，否则假告警会掩盖真问题。**
  3. **统计"篇数"时要决定是否含非 `University/` 的 md**（Vault 里通常还有 `.workbuddy/`、`_archive/` 下的笔记副本，一律排除，否则篇数对不上）。
- **顺手做的第三项审计：路径引用要写全**（从 Vault 根起）。脚本口径：用反引号包住、且含 `课件/`、`课堂录音纪要/`、`课本/`、`作业讲解/` 的字符串，**若首段就是这些子目录名（而非课程名）**，即为"裸子目录"写法，应补成 `课程名/子目录/...`（实测清理 9 处）。
- 文件命名：默认档 `LXX - Topic.md` / `Concept Name.md` / `Week XX Review.md` / `Course Hub.md`；**全中文档**（§2 ④）`第 0X 讲 - Topic.md` / `概念名.md` / `第 XX 周复习.md` / `课程主页.md`。简洁、稳定、易搜索，**不频繁改名**；**改名前先确认是"按课生效"还是"全库推行"**。
- ⚠️ **任何改名都是"高阶危险操作"**，必须成套执行：`cp -R` 备份 → 改名 → 改正文 → **改所有指向旧名的链接（含跨课程页）** → 全库链接审计 → 与备份对账。**改完不清算链接，就是给未来的自己埋雷。**
- **不做无效链接**：未建立的 Lecture/Concept **不写 `[[ ]]`**，改用纯文本 +（计划中）标注，避免 Graph 里一堆未解析节点。
- **禁止跨课程 Concept Link**（各课概念体系互不相通）。⚠️ **但两类跨课程链接是允许且鼓励的**：① 指向 **Course Hub / Lecture** 的链接（用于记录"课程编排事实"，如"某讲用到的工具与另一门课同一周的讲是同一套"）；② 指向跨课程页（总览页 / 课表页）的链接。前提是**一律路径限定**。

---

## 5. Lecture Note 结构

frontmatter：`course / lecture / date / topic / status / source / tags`（`status` 用第 3 节的取值）

正文（标签用中文）：

1. **快速回顾** — 最重要之一。5–10 个 bullet，让人 3–5 分钟重新理解这节课。**不写成长篇文章。**
2. **核心概念** — 只保留真正重要的；**定义**用课程材料中的定义重写（中文主述 + `> PPT 原文：` 附注）；**现实案例**只在 PPT/老师提供时记录，**没有就不造**
3. **核心框架与模型** — **用途** / **构成** / **如何使用**；重要原图引用原 PPT 页码
4. **老师补充** — 只记录 PPT 中没有、老师补充的重要内容（解释 / 现实案例 / 对比 / 澄清 / 例外 / 常见错误）。**不保存所有口语。** 无录音时明确写"本讲无课堂录音，不记录"
5. **评估信号** — 只有明确证据才记录；没有就写 `未发现明确的评估信号。`
6. **关联** — 只建 Lecture ↔ Concept 与必要的 Lecture ↔ Previous Lecture；关系动词用中文（承接 / 引入 / 应用）

图片引用：`![[原文件全名.pdf#page=N]]` + 一行纯文本图注（含原 PPT 页码）。**不要**引用装饰图、logo、无学习价值的 stock image、纯标题页。若环境不支持嵌入，保留 `来源：Week03-Slides.pdf, 第 XX 页`。

⚠️ **课件页码 ≠ PPT 页码**：部分课件 PDF 含**封面页**，于是 **PDF 第 N 页 = 原 PPT 第 N−1 页**；也有课件两者页码一致。**嵌入语法填 PDF 页码，图注同时标出两者**（如"PDF 第 3 页 ＝ 原 PPT 第 2 页"）。判断方法：把 PDF 每页残留的页脚页码与提取文本里的 `===== PAGE n =====` 对照。**引用前必须核对**，否则定位整体偏一页。

---

## 6. Concept Note 创建门槛

**不为每个术语建 Note。** 须满足至少一项：

1. 在多节 Lecture 出现 2. 是课程核心 Framework / Model 3. 对理解后续课程重要
4. 老师明显强调 5. 与 assessment 明确相关 6. 是该课程核心专业概念

建前**必须查重**。名称相似不等于同一概念——若课程材料视其为不同概念则分别保留；明显是同一概念的不同写法则复用已有 Note。

**合并两条 Concept 时**：新文件用组合名（如 `Lorenz Curve and Gini Coefficient.md`），frontmatter `aliases` 保留两个原名，正文合并后同步更新所有入链，旧文件移入归档（见第 9 节），**不硬删除**。

格式：`English Concept Name（中文）` → 定义 → 核心理解 → 构成要素 → 框架 · 图示（引用原图）→ 出现于 → 老师补充（仅有 transcript 证据时）→ 评估相关性（仅有明确证据时）

---

## 7. Weekly Review 结构（固定四段 + 收尾）

1. **一、本周讲了什么** — `讲次 XX：……` 简洁结构，3–5 分钟知道本周讲了什么
2. **二、核心概念** — `[[Concept]]` + 一句话说明为什么重要，**不复制整个 Concept Note**
3. **三、讲次之间的关系** — 用箭头图解释顺序与"为什么课程按这个顺序讲"，只能依据材料能支持的关系
4. **四、必掌握的框架** — 本周必须掌握的 Framework / Matrix / Model / Formula / Process，配图引用，一句话说明用途
5. **考核关注点** — 权重与复习精力分配

**不需要**：长篇总结、大量细节、重复 Lecture Note、自行预测考试题。
**只为本当周实际有课程内容的课程生成。**

⚠️ **一门课每周只有一讲时**，"讲次之间的关系"段无内容可写——改为写**本讲内部逻辑链**，或与用户商量改成"两讲合并一次"。

---

## 8. Course Hub 结构

课程简介 / 课程目标（提取 syllabus；**没有就写 `⚠️ 课程材料未提供足够信息，无法清楚解释这一点。` 不自行补写**）/ 考核方式表（考核项 | 权重 | 截止 | 状态 + 合计行）/ 课程进度（周次 → 主题 → 状态，或模块制，取决于材料给的是周次还是模块）/ 讲次索引 / 核心概念 / 周复习 / 课程信息 / 原始材料位置。

- Hub **只是导航页**，不做成第二份 Lecture Note。
- 讲次索引标注状态：`[[L01 - XXX]] — 已讲授` / `L03 — 课件已到位` / `L04 — 计划中`
- 核心概念 **只列已真正建立的** Note，不从 syllabus 一次创建几十个空条目。
- 有学期日期时，**优先推算并写明学期周次锚点**（用材料明述的"某日期 = 第几周"反推，并用日历核对星期），再据此标出考试撞车等排期风险。
- 英文版 syllabus 的 **Course Description 段落**：不要直接贴英文原文，改写为中文段落 + `> syllabus 原文：…` 引用。

### 跨课程页（可选，**须用户确认**）

Hub 只做单课程导航，但**只有把所有课拉到一起看，才能发现"第 9 周连着两场大考""周四连排 5 段课"这类问题**。若用户同意，可另建跨课程页。**目前有两张，分工严格不重叠**：

| 页面 | 只回答 | 内容 |
|---|---|---|
| `考试与截止日期总览.md` | **什么时候考、占多少分** | 考试日期、权重、作业/项目截止（logistics） |
| `学期课表.md` | **每周什么时候上什么课 + 笔记是否完整** | 周视图网格、节次↔钟点、时段明细、学期跨度、**课表 × 笔记覆盖矩阵**、讲次缺口清单 |

- **两页之间只留一条链接互相指向**，内容不重叠（否则改一处要改两处，必然不同步）。
- **都不含概念链接**，因此不违反"禁止跨课程 Concept Link"。
- **无 syllabus 的课程明确写"无信息"，不要从其他课程类推。**
- ⚠️ **新增跨课程页属"超出原规格"的动作**，必须在页面顶部显式写明"由用户于 YYYY-MM-DD 确认增设"，并同步更新已经引用它的页面。
- 课表页的完整做法见 §15。

---

## 9. 破坏性操作与归档

- **可自动执行**：创建/更新 Lecture、Concept、Course Hub、Weekly Review，添加链接，增量补充，修小格式，**批量中英标签改写**（改前先备份）。
- **必须先问**：删除文件、大规模移动/重命名、重构 Vault、合并 Concept、覆盖大量笔记、改目录结构。
- **不硬删除**：把文件移入 `<Vault根>/.workbuddy/_archive/<日期>-<说明>/`（dot 目录 Obsidian 默认不索引，等于退出 Vault 但可还原）。
- 增量更新时**不重写整个 Vault**。补充 transcript 时**更新原 Lecture**，不新建 `L03 Transcript Version.md`。
- **批量改写前必须整目录备份**：`cp -r University .workbuddy/_archive/<日期>-<说明>-backup/`。

---

## 10. 提取工具链

```bash
# 安装依赖
pip install pdfplumber python-docx olefile

# 用法：extract.py <源文件> <输出txt>
python tools/extract.py "课件/lecture 1.pdf" /tmp/L01.txt
```

- **PDF** → `pdfplumber`（逐页提取，输出含每页 `images=N`，便于判断是否扫描件——**若整份都是 `images` 多而文本极少，说明是扫描件，需 OCR，本工具链不覆盖**）
- **.docx** → `python-docx`（保留 Heading 层级与表格）
- **老式 .ppt（二进制 OLE）** → `olefile` 读 `PowerPoint Document` 流，**递归下降**遍历，提取 `TextCharsAtom`(0x0FA0, UTF-16LE) 与 `TextBytesAtom`(0x0FA8, cp1252)，去重后输出
- **老式 .doc（Word 97-2003，也是 OLE 复合文档）** → ⚠️ `extract.py` **不支持**。macOS 上直接用系统自带的 **`textutil`**（无需安装任何东西）：

  ```bash
  textutil -convert txt -output "/tmp/out.txt" "<源文件.doc>"
  ```

  实测有效（4 页、417 词的作业文档完整提取）。判断是否属于这一类：`file <文件>` 返回 `Composite Document File V2 Document ... Name of Creating Application: WPS Office / Microsoft Word`。⚠️ 注意 `.doc` 与 `.docx` 完全是两种格式，**不要**把 `.doc` 传给 `python-docx`，会直接报错。

### ⚠️ .ppt 提取的三个坑（必读）

1. **必须递归进容器**。该流是嵌套记录结构，文字块藏在 Slide(0x03E8) 等容器**内部**。判据：`(rec_ver_inst & 0x000F) == 0x000F` 即容器。若遇容器按 `rec_len` 整块跳过，会对**所有** .ppt 输出 **0 字符**（本流程实际踩过，代价是怀疑了整个工具链）。
2. **逐页分组不可靠**。不同 deck 分出的 `SLIDE n` 数量差异极大（0 / 1 / 8 页不等）。**正文完整可靠，但不要依赖 SLIDE 编号定位页码。**
3. ⚠️ **`.ppt` 不要用 `![[...]]` 嵌入**。Obsidian **无法渲染**老式二进制 `.ppt` 与 `.doc`——嵌入只会得到一个打不开的块。正确做法：**改用反引号写纯文本路径 + 一句"需用 PowerPoint / WPS 打开"的提示**。只有 `.pdf`（和图片）才适合 `![[file.pdf#page=N]]`。

macOS 上通常**没有** LibreOffice/soffice；WPS 若是 MAS 沙箱版也无法命令行转换——所以自写解析器是必要路径，不是备选方案。

### 陷阱检查

- 用户提供的"课堂录音纪要"文件夹可能是**课件同名文件的重复副本**。用 `md5` 校验，一致则说明该课**没有真实纪要**，只能标 `课件已到位`（**只标注，不删文件**）。
- ⚠️ **同一门课可能"假纪要"与"真纪要"并存**：该课后来补了真实纪要后，**旧副本仍留在原处**——两者必须分别说明，不得因为"有真纪要了"就放松对旧副本的标注。
- 课件文件名可能与内容章节号**不对应**（实测：文件名 `1.2` 内含 `§1.1`，文件名 `1.3` 内含 `§1.2`）。**以内容章节标记为准，不要按文件名推测。**
- ⚠️ **同一门课的不同章，规律可能相反**：实测第 1 章的文件名 = **小节号 + 1**，第 2 章的文件名 = **小节号本身**。⇒ **不要用上一章推出的规律去套下一章**；**判据只有一个：打开 PDF 第一页看正文标题**。在 Hub 的"课程进度"表里**两章的规律都要写清楚**，别只写一条。
- ⚠️ **可能同时存在两套章节编号**：课件文件名一套（如 `Ch01`–`Ch05`），老师口头引用教材时另一套（如"第九章到第十三章"）。**两者不是同一套编号**，须在 Hub 顶部单列警示，且**不为缺教材名的课推断教材**。
- PDF 中的**竖排文字表格**提取会错位（数字列串行）。此时宁可不列该列，也不要输出错位数字。

---

## 11. 语言改写工具链（relang 系列 / zhify 全中文化系列）

`tools/relang.py` 是一个**通用批量改写引擎**，配合外置规则文件使用。它把笔记从"英文标签 + 直贴英文原句"改成第 2 节的规范。

**设计要点（重要）**：

- 替换表写成 **精确对**（`exact`）+ **全局规则**（`global`）两段；**先跑精确、再跑全局**，否则精确对会被全局替换提前破坏。
- 每处替换**报告命中数**，未命中单独列出——保证"改了什么"可审计，不会静默漏改。
- 支持 `--dry` 试运行，确认 0 未命中后再落地。
- 脚本**幂等可重跑**，改坏了可从备份还原。

用法：

```bash
cp tools/lang_rules.example.json <vault>/.workbuddy/tools/lang_rules.json
# 按你的课程修改 rules 文件（把 exact 段替换为本课程的实际改写对）
python tools/relang.py --rules <vault>/.workbuddy/tools/lang_rules.json --root <vault>/University --dry
python tools/relang.py --rules <vault>/.workbuddy/tools/lang_rules.json --root <vault>/University
python tools/relang.py --rules <vault>/.workbuddy/tools/lang_rules.json --root <vault>/University --stats
```

`lang_rules.example.json` 里已内置**全量通用标签映射**（标题 / 表头 / 状态值 / 字段标签 / 占位句），可直接复用；`exact` 段给了 2 条示例说明格式。

> `tools/relang2.py`、`tools/relang3.py` 是**后续轮次的变体**（第二轮处理结构标签之外的英文内容层、第三轮清理残留英文指代），规则是**一次性内联**的，作为"怎么把同一条思路推进到内容层"的参考保留，不建议直接复用。

**易错点**：Python 里字符串隐式拼接在遇到变量（如 `PPT + "..."`）后会断掉，`'\n'` 直接换行接变量是语法错误，必须写 `'\n' + PPT + ...`。

### 11.1 全中文化工具（`zhify.py` + `zh_check.py`）

§2 ④「全中文档」的落地工具。**relang 解决的是"结构标签中文化"，这两个解决"连文件名一起中文化"，风险量级完全不同——它会动链接。**

**`zhify.py` —— 转换器（同时改文件名与正文）**

```bash
PY=python3
cd tools
$PY zhify.py                                   # 空跑，只报命中
$PY zhify.py --apply                           # 落盘
ZH_VAULT=/tmp/zh_test $PY zhify.py --apply     # 沙盒验证（不碰真 Vault）
```

内部结构（照抄即可复用到别的课）：

- `VAULT`（可用环境变量覆盖）、`COURSES`（**只处理指定课，绝不扫全库**）
- `RENAME` 改名表 + `NEED_PATH`（改名后必须路径限定的基名）
- `PROT` 保护段（**必须含 `\[\[[^\]]*\]\]`**，见 §2 ④ 坑 1）、`RULES` 术语规则、`FM_VALUE`（frontmatter 值）、`ALIAS_DROP`（要删的英文别名）、`EXACT`（整行精确替换，兜住 H1 标题）、`MASKS`（文献标题哨兵遮蔽）
- 函数：`fix_link()` / `convert()` / `apply_rules()` / `_fixb()`（`\b` 边界重写）/ `_delparen()`（删英文括注）

⚠️ **执行顺序不能换**：MASKS 遮蔽 → 删英文括注 → 跑术语 RULES → 首次写全文献名、其后改「该研究」 → 还原遮蔽。**顺序错了，删括注会把还原后的英文再删一遍。**

**`zh_check.py` —— 收尾四步核查（每次转换后必跑）**

```bash
$PY zh_check.py <vault 根> <课程名> [<课程名> ...] --backup .workbuddy/_archive/<日期>-zh-backup
```

| 步骤 | 作用 |
|---|---|
| ① `aliases` 清理 | 删 frontmatter 里的纯英文别名（正文规则碰不到 frontmatter） |
| ② 完整性对账 | 与改前备份**逐文件**比对 (行, 嵌图, 公式, 链接) 多重集，四项须一致 |
| ③ 重复译名扫描 | 找双重替换产物（如「盈余后漂移盈余后漂移」） |
| ④ 全库链接审计 | 未解析 / 歧义 / 路径失效，三项均须为 0（口径见 §4） |

⚠️ **对账出现 +N 行 / +N 公式不等于出错**——若收尾修正步骤**有意**补了内容，必须能**指名解释每一处差异**；**解释不出来的差异就是规则改坏了东西**。

⚠️ `--backup` 的相对路径按 **Vault 根**解析（脚本可能在任意 cwd 运行）；课程名只取位置参数，`--backup` 及其值会被摘除，**不要写成"课程名列表末尾跟 `--backup`"**。

---

## 12. 判断"什么值得记"的最终问题

每次落笔前问：

> **"未来考试前快速复习时，这条信息是否值得再次看到？"**

答案明显为否 → 不保存。

---

## 13. 禁止清单

❌ 把 transcript 全部复制进 Obsidian ｜ ❌ 每页 PPT 都总结 ｜ ❌ 每个专业名词都建 Concept Note
❌ 加入课程外知识 / 联网补充 / 用背景知识"通常是这样" ｜ ❌ 猜考试内容
❌ 为 Graph View 建大量链接 ｜ ❌ 跨课程 Concept Link ｜ ❌ 创建大量空文件夹
❌ 重复建立同一 Lecture ｜ ❌ 因新材料出现就重写整个 Vault
❌ 没有证据却写"老师强调" ｜ ❌ 把未来 PPT 当成已上过的 Lecture
❌ 用文件时间戳反推上课日期 ｜ ❌ 凭课表外的推测把一门课"补进课表" ｜ ❌ 把课表节次外推成钟点
❌ 把他人版权材料（课件 / 电子课本 / 录音）推送到公开仓库

**语言层面另加：**
❌ 小节标题、表头、字段名留英文 ｜ ❌ 英文定义句整句当前置主述（应降级为 `> PPT 原文：` 附注）
❌ 同一个意思中英各写一遍 ｜ ❌ 把术语缩写（MSB / DWL / ELR）翻译成中文导致考试时认不出

**仅全中文档（§2 ④）反向约束：**
❌ 普通术语（`income change` / `Winsorize` / `EMH`）仍留英文 ｜ ❌ **凭印象自创译名**（必须先回课件取证：是「贝努利」不是"伯努利"，是「分布律」不是"分布列"）
❌ 把专名（`Ball & Brown` / `CSMAR` / `Stata`）或公式记号也硬译成中文 ｜ ❌ 未经用户逐课指定就把**其他课**也改成中文（口径按课生效）

---

## 14. 每次工作后的报告格式

```
完成
课程：XXX
讲次：XX

更新
- LXX - XXX
- 课程主页

新建
- [[Concept A]]

主要内容
- XXX

考核信号
- 无 / 老师明确强调 XXX

需要你处理
- 无（只有真的需要用户处理时才写）
```

**不要每次给长篇工作汇报。**

---

## 15. 课表融入与笔记完全性审查

**触发场景**：用户给出教务课表（截图 / CSV / 手输），要求"**把课表融入笔记**""**审查笔记是否完全**"，或想看清"这门课我到底缺了哪几讲"。

这类任务的价值不在"把课表抄进 Obsidian"（那用不着 Skill），而在**拿课表当基准，去反查笔记缺口**。

### 15.1 先澄清，再动手

⚠️ 这一步不能省，否则必返工。至少问清七个分支：

1. **范围**：课表上的课，哪些要建笔记？（实测：8 门专业课全纳入，5 门非专业课**只入课表、不建笔记**）
2. **课表外的课怎么办**：用户口头提到但教务课表未列的课，是补进课表还是另记？
3. **载体**：新建独立页，还是并进既有的跨课程页（见第 8 节）？
4. **钟点**：只写有出处的，还是按规律外推？
5. **可视化程度**：纯表格 / 表格 + 甘特 / 导出图片？
6. **既有笔记动不动**：只回填信息，还是允许改名、挪目录？
7. **缺口补建口径**：哪些缺口补、哪些只登记？

### 15.2 课表数据的合并规则

- **来源优先级**：教务课表（书面）> syllabus（书面）> 用户确认 > 推测。**推测一般不采纳。**
- ⚠️ **教务课名 ≠ 目录名**：教务课表里的课名常带后缀或更长（如 `某课程（全英语）`、`某课程与数理统计`），而 Vault 目录名是简写。**默认不改目录名**，只在各课 Hub 的"课程信息"里登记一行教务课名。
- ⚠️ **同一间教室可能有两种叫法**（如 `某某楼 313` / `某教 313`）——属同一处的两种写法，**页面内统一采课表写法**，并在口径说明里点出。
- ⚠️ **教师姓名冲突**：若 syllabus / 录音转写 / 教务课表给出不同名字，**以教务课表为准**；录音里的名字常是转写错误（实测：录音转写的姓名与教务课表不符，教务课表才是对的）。
- ⚠️ **同一教师可能出现在两门课**：这不是笔误，**不要在"去重"的直觉下删掉一个**，要在页面里显式写明"材料明述的重合"。

### 15.3 节次 ↔ 钟点：只写锚点，不外推

教务课表只给"第 N 节"，不给钟点。**必须先在材料里找有出处的锚点，然后只补锚点覆盖的课；其余写"未载明"。**

本项目实测的 4 个锚点（**不同学校/学期会不同，须逐次取证，不要照抄**）：

| 节次 | 钟点 | 出处 |
|---|---|---|
| 1–2 | 08:00–09:40 | 用户提供 |
| 3–4 | 10:00–11:40 | 用户提供 |
| 3–5 | 10:00–12:35 | syllabus 明述 |
| 7–9 | 14:00–16:45 | syllabus 明述 |

- ⚠️ **上午与下午的课间时长可能不同**（实测：上午 10 分钟、下午 15 分钟）——由锚点反推可验证，**不要假设全天上下午一致**。
- ⚠️ **不外推的理由**：由"3–5 节 = 10:00–12:35"推不出"7–8 节是几点到几点"，因为上下午课间不同、且中间有午休边界。**推错会让整张表看起来精确、实则全错**——比写"未载明"糟糕得多。

### 15.4 课表页的九节结构

1. **周视图网格**（节次 × 周一~周五）——一眼看出负载分布。**加粗＝该课在 Vault 有笔记落点**，另一种记号标非专业课。
2. **节次 ↔ 钟点换算表**（只列锚点 + 换算依据 + "不外推"声明）
3. **时段明细表**（每条：星期·节次 / 钟点 / 教务课名 / 教师 / 地点 / 周数 / Vault 目录 / **来源**）——"来源"列不能省，它是整张表的可信度凭证。
4. **学期跨度**（mermaid 甘特，见 15.6）
5. **课表 × 笔记覆盖矩阵**
6. **讲次缺口清单**（见 15.5）
7. **待补情报清单**
8. **风险**
9. **维护说明**

⚠️ 网格表下面**必须写"从这张网格能直接读出的三件事"**——纯表格用户不会自己读（本项目读出的结论：周四连排 5 段课、周一被两门只有周一有课的课占死、周五上下午之间空 6 节）。

### 15.5 三层审查输出（"笔记是否完全"就靠这三层）

| 层 | 判据 | 输出 |
|---|---|---|
| **① 覆盖矩阵** | 课表每门课，在 `University/` 下有没有对应目录与主页 | 有 / 无；"无"要区分是**有意不建**还是**漏了** |
| **② 讲次缺口** | 已上过几次课 vs 有纪要几次 vs 已建讲义几讲 | 缺口清单，**必须分两类**（见下） |
| **③ 待补情报** | 明确缺的事实（地点、syllabus、钟点、考核方式） | 可直接照着去问老师的清单 |

**缺口必须分两类，否则会做出错误的承诺：**

- **甲类 = 有录音可补** ⇒ 本轮就补出讲义。
- **乙类 = 无录音，按第 3 节规则不得建讲义** ⇒ **只登记、不建**，写明"无纪要，等录音"。

⚠️ **判读规则必须写进页面**，否则读者会把"课件已到位"误读成"老师讲过"：

- **有课件 ≠ 已讲授**（实测：某课有 6 讲课件，实际只上了 2 讲）。
- **有材料 ≠ 有笔记**（用户可能根本没要求这门课建笔记）。
- **一次课 ≠ 一讲**（第一讲可能横跨两周）。
- **一次课 ≠ 一次录音**（可能缺录）。

### 15.6 可视化：Markdown 网格 + mermaid 甘特

- **周视图网格**用普通 Markdown 表格（节次 × 星期），**不要用 HTML `<table>`**——Markdown 表在 Obsidian 里可编辑、可搜索、可被其他笔记引用。
- **学期跨度**用 mermaid 甘特图，Obsidian **内置渲染、无需插件**：

  ````markdown
  ```mermaid
  gantt
      title 学期跨度
      dateFormat YYYY-MM-DD
      section 专业课
      某课程 :a1, 2026-09-08, 112d
      section 里程碑
      期中考试 :milestone, m1, 2026-11-05, 0d
  ```
  ````

- ⚠️ **不主动导出图片**：内嵌图不可编辑，课表一改就不同步。除非用户明确要图片。
- ⚠️ **mermaid `gantt` 只画跨度**，不承担精确到日的排期；精确日期仍写在 15.4 的时段明细表里。

### 15.7 回填既有笔记的纪律

- **只回填事实**（教师 / 节次 / 地点 / 教务课名），**不改目录、不改文件名**——除非用户明确要求（改名的代价见第 4 节）。
- 每门课 Hub 的"课程信息"段补一行指向课表页的链接，让课表页与各课互达。
- ⚠️ **回填会暴露旧错误**：实测回填时发现某课 Hub 的教师姓名是错的（见 15.2）——**发现即改，并在维护记录里写清改了什么**。
- ⚠️ **回填也会暴露"主页里写的钟点是猜的"**：若某处钟点原本没有出处，正好借此补上或改为"未载明"。

### 15.8 收尾（缺一都会留下隐患）

1. 全库复跑**链接审计**（第 4 节）——新增页面会产生大量跨课程链接。
2. 全库复跑**路径引用审计**（第 1 节）——回填时写下的 `` `课件/…` `` 这类串要与实际结构一致。
3. 更新跨课程页的分工说明（第 8 节），避免两张表内容重叠。
4. 课表页的新事实**同步进长期记忆**（教师、节次、地点、无 syllabus 的课），否则下次又要重新问一遍。
